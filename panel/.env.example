# Copy to panel/.env to configure the app's connection to the gateway.

# Gateway base URL (REST + WebSocket are derived from it).
VITE_GATEWAY=http://127.0.0.1:8090

# API token — only if the gateway runs with PANEL_API_TOKEN set.
# VITE_GATEWAY_TOKEN=
