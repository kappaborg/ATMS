# tools/.safety_clock_legacy.txt
#
# Files in the ATMS safety scope (per ADR-0017 §rule) that contain legacy
# wall-clock callsites awaiting a Phase A2 refactor. Listed here so the
# safety-clock lint does not block PRs while the refactor is staged.
#
# Lines: repo-relative paths (one per line). Blank lines and `#` comments
# are ignored.
#
# When a file is refactored and its callsites are moved to monotonic_ns()
# or marked `# noqa: ATMS-CLOCK` for legitimate display-only uses, REMOVE
# its entry from this file. The lint will then enforce the rule on it.

# --- ai-perception (A2 pending) ---
services/ai-perception/src/tracking/object_tracker.py
