# Test/dev dependencies for traffic-controller.
# Production deps are in requirements.txt.
-r requirements.txt
pytest>=8,<10
pytest-asyncio>=0.23
hypothesis>=6.100
coverage>=7.15.2
# Used by integration/test_failsafe_chaos.py to spin up real Kafka.
# Requires Docker on the test host. Tests are skipped when Docker is absent.
testcontainers[kafka]>=4
prometheus_client>=0.19
# starlette.testclient (integration tests) requires httpx.
httpx>=0.27,<0.28  # fastapi 0.104.1 -> old starlette TestClient needs httpx app= (removed in 0.28)
