apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ai-perception
  namespace: atms-system
  labels:
    app: ai-perception
spec:
  selector:
    matchLabels:
      app: ai-perception
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s

